name: Keep Render App Alive

on:
  schedule:
    # Runs every 10 minutes
    - cron: '*/10 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl your website
        run: curl -s -o /dev/null https://taskmanagerbyjayptl.onrender.com/
